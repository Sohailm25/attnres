# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-18
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: there is no new broad rerun to launch right now. The main story is already in the saved Gemma oracle synthesis artifact, and the next core lane is `resattn-m6r`, the first Phase 6 pilot router comparison on the exported dataset.
- Experimental status: the center of gravity stays on the primary Gemma oracle lane. Tool-breakage is frozen at a truthful boundary, and pilot-only per-token export is now landed for the full `256`-prompt Gemma pilot surface.
- Critical reminder: do not drift back to pooled reruns, donor-remap-by-inertia, moon redesigns on the old `v4` surface, or frozen Figure 8 rescue work. The current blocker is no longer missing export; it is making the first truthful `h_1[t]` versus `h_4[t]` router-training comparison.
