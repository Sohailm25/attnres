# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-2s0` is now green on the primary spine and the next overall scientific priority is `resattn-7cs`, with `resattn-1lk` now a secondary strategic decision rather than the top paper-shaping blocker
- Experimental status: the `96 / 128` `registry_v4` campaign on `gpt2-xl` clears the development-model oracle-loss gate strongly against uniform and the preregistered nulls; the strongest remaining oracle-alpha paper-shape weakness is that this positive result is still development-model-only; the primary `google/gemma-2-2b` spine now passes the same model-backed reconstruction smoke exactly on local MPS, so the next oracle question is actual primary-model feasibility rather than backend readiness; the raw prereg-scale pattern slice survives grouped-view and resampling controls but remains too weak and too `k = 2`-dominated for the prereg block-structure gate; grouped-source compressions reveal stronger coarse source-type routing structure above matched random controls; the Gemma factual-recall tool-breakage lane remains a bounded mixed result because routed traces degrade the original-model baseline but not more than the prompt-permuted dynamic control; the widened compact-subword `wikitext-103` Figure 8 lane remains operationally real but strategically frozen for strong claims on the current tiny proxy; and the aligned-Gemma mediator-conditioned routing artifact now shows large final-layer refusal-trajectory shifts under refusal-direction interventions even though the mediator-active confirm partition still collapses exactly onto refusal prompts
- Critical reminder: the biggest remaining end-state risk is still that the strongest positive oracle-alpha result lives on the development model; keep the oracle-alpha framing honest, preserve the KL-primary tuned-lens metric hierarchy, treat the grouped-view pattern result as coarse-structure evidence rather than a block-structure pass, keep the Figure 8 lane in freeze-or-escalate territory rather than iterative rescue, and do not overstate the safety lane while the current mediator partition remains role-collapsed
