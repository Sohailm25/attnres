# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-18
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-138` is now the next bounded tool-breakage execution issue. The next scientific pass should run the locked `tool_breakage_factual_recall_v5` confirm baseline and keep the read stratified by intended route mode as well as by family.
- Experimental status: the center of gravity stays on the primary Gemma oracle lane, and the factual tool-breakage bridge now has a live route-mode-aware surface with a positive pilot. The main execution question is no longer whether `v5` has signs of life; it is whether that narrowed route-mode read survives on the confirm split.
- Critical reminder: do not drift back to pooled `v4` reruns, moon-led redesigns, or frozen Figure 8 rescue work. `resattn-a1w` stays a moon-specific sidecar, and Figure 8 stays frozen.
