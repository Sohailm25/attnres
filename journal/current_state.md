# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-18
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: the broad factual frame audit is done, the simple aggregation comparison is done, and the width-only capacity comparison is now done. The main active implementation next step is `resattn-zic`, the bounded router-family comparison for pilot router distillation. If a future factual sidecar is needed, it should be `resattn-0kc`, the small residual author `novel_title` split, not another broad family-versus-frame pass.
- Experimental status: the center of gravity stays on the primary Gemma oracle lane. The strongest live story is still primary-model effective depth mixture plus factual family-plus-prompt-frame-conditioned route modes on the strongest stratum. Tool-breakage is frozen at a truthful boundary, Figure 8 stays frozen, and the Phase 6 pilot is now blocked by router family rather than export, target geometry, the first obvious aggregation alternative, or a simple width increase.
- Critical reminder: do not drift back to pooled reruns, donor-remap-by-inertia, moon redesigns on the old `v4` surface, frozen Figure 8 rescue work, more arbitrary aggregation churn, or another blind width sweep on the same fixed baseline. The honest next move is the router-family comparison on the saved pilot export.
