# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-111` has now isolated width on the compact-subword Figure 8 proxy; the next follow-up is `resattn-jci`, which should keep the widened compact-subword regime fixed and test optimization horizon only
- Experimental status: the `96 / 128` `registry_v4` campaign on `gpt2-xl` clears the development-model oracle-loss gate strongly against uniform and the preregistered nulls; the first sequence-level pattern slice shows weak above-random but below-threshold structure; the Gemma-2 tuned-lens pilot remains KL-strong and answer-token-weaker; the locked confirm Gemma factual-recall baseline reproduces broad KL degradation under routing with `7 / 8` tuned rank-range increase and `5 / 8` tuned final-rank worsening; the dynamic counterfactual shows routed traces remain worse than the fixed `pilot_mean_alpha` control but not worse than the prompt-permuted dynamic control on the tuned KL surface; the char-level `8`-block Wikitext proxy still beats its matched baseline on eval loss while showing mixed Figure 8 metrics; the first compact-subword proxy improved the paper-facing metrics but lost the longer-horizon baseline comparison; and the widened compact-subword follow-up improves deep embedding persistence again (`0.1515`) while still losing to the matched baseline (`6.6496` versus `6.5899`) and keeping the entropy ordering inverted
- Critical reminder: keep the oracle-alpha framing honest, preserve the KL-primary tuned-lens metric hierarchy, and do not treat either local AttnRes proxy artifact as a Figure 8 alignment pass until the proxy metrics themselves look credible and the proxy remains competitive with its matched baseline
