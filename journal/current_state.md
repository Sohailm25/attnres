# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-19
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: the broad factual frame audit is done, the saved reasoning/math route-mode audit is now also done, the simple aggregation comparison is done, the width-only capacity comparison is done, the first router-family comparison is done, the supervision-granularity audit is done, the richer-supervision comparison is done, the family rerun under all-token supervision is done, the residual author `novel_title` split is bounded as a lexical-surface sidecar, the bounded tokenwise teacher-target comparison is done, the exact tokenwise-oracle subset comparison is done, and the exact-teacher mismatch audit is now also done. The main active implementation step is now `resattn-y4m`, the embedding-isolated block-compressed target comparison.
- Experimental status: the center of gravity stays on the primary Gemma oracle lane. The strongest live story is still primary-model effective depth mixture plus strong grouped structure plus stratum-conditioned raw-source structure, with factual recall already sharpened to family-plus-prompt-frame-conditioned route modes and reasoning/math now sharpened to operation-dominated but still frame-conditioned route modes. Tool-breakage is frozen at a truthful boundary, Figure 8 stays frozen, and the Phase 6 pilot now has a sharper negative boundary: `all_tokens_target_mse` remains the safest broader baseline, the widened MLP only helps modestly, the support-only next-token mask is a tie on the larger full split and only a modest positive on the bounded exact subset, and the new mismatch audit says exact-tokenwise teachers mainly fail because they disagree too much within prompts while also averaging to a more diffuse target than the scored sequence-level oracle. The next honest Phase 6 design branch is therefore compression-over-sparsity with embedding isolated as an explicit hypothesis, not more arbitrary span masks, more exact-tokenwise teachers, or sparse approximations. Factual recall remains the main structured-interpretability center and bridge lane.
- Critical reminder: do not drift back to pooled reruns, donor-remap-by-inertia, moon redesigns on the old `v4` surface, frozen Figure 8 rescue work, more arbitrary span masks, another blind width sweep, another blind head-family tweak, or a full tokenwise export redesign after `b4h`. Also do not let Phase 6 recency crowd out saved-artifact oracle analysis when the strongest science is already on the primary Gemma spine, do not let the reasoning/math secondary positive displace factual recall as the main bridge lane, and do not misread AttnRes block engineering choices as proof that our frozen-model raw-source clusters should naturally come out to `~8`.
