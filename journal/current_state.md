# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-2sb` is now green and the next overall scientific priority is `resattn-5eo`, the primary-model Gemma routing-regime comparison, with `resattn-1lk` and `resattn-mo5` still the next cross-lane strategic questions
- Experimental status: the biggest oracle-paper weakness from the last external review is now materially reduced because the primary `google/gemma-2-2b` spine not only passes exact model-backed reconstruction, a bounded `2`-prompt oracle slice, the full saved `8`-prompt pilot stability suite, and the held-out predictiveness check on the saved `96 / 128` `registry_v4` split, but now also has its first prereg-scale pattern read; that primary-model pattern result is mixed in a specific way: grouped views are stronger than on `gpt2-xl` (`source_type` `0.6731` versus random `0.5569`, `depth_thirds_by_type` `0.3858` versus random `0.2305`), while the raw-source view is still weak and below the matched random silhouette (`0.1084` versus `0.1598`), so the honest update is stronger coarse source-type structure without a raw block-structure pass; the Gemma factual-recall tool-breakage lane remains a bounded mixed result because routed traces degrade the original-model baseline but not more than the prompt-permuted dynamic control; the widened compact-subword `wikitext-103` Figure 8 lane remains operationally real but strategically frozen for strong claims on the current tiny proxy; and the aligned-Gemma mediator-conditioned routing artifact now shows large final-layer refusal-trajectory shifts under refusal-direction interventions even though the mediator-active confirm partition still collapses exactly onto refusal prompts
- Critical reminder: preserve the distinction between grouped coarse structure and raw block structure; the primary model now has a real positive oracle and grouped-pattern story, but the prereg raw `~8`-cluster gate is still unpassed; the next right move is the mandatory regime comparison on the primary model, not another small clustering variation; keep the Figure 8 lane in freeze-or-escalate territory rather than iterative rescue, preserve the KL-primary tuned-lens metric hierarchy, and do not overstate the safety lane while the current mediator partition remains role-collapsed
