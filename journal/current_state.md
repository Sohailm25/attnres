# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-18
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-1lk` is now green and the next overall scientific priority is `resattn-mo5`, the smallest safety prompt-surface broadening that can break the current role-collapsed mediator partition
- Experimental status: the primary `google/gemma-2-2b` spine now has a real full-lane oracle story rather than only feasibility fragments: exact reconstruction, bounded oracle smoke, pilot stability, held-out predictiveness, grouped pattern analysis, and the prereg routing-regime comparison. That regime result is clean and important: softmax-constrained routing improved over uniform by `+2.5525` nats, unconstrained improved by `+2.0637`, top-k improved monotonically from `+0.0115` at `k = 2` to `+1.4664` at `k = 26`, and softmax beat every alternative on all `128` confirm prompts. The strategic Figure 8 decision is now also explicit: the current local proxy remains operationally real but stays frozen at the descriptive boundary for strong-claim purposes because the faithful rescue path on that proxy family is exhausted and no materially more faithful proxy path is currently concrete enough in the repo to execute immediately. The Gemma factual-recall tool-breakage lane remains a bounded mixed result because the prompt-permuted dynamic control is at least as damaging as the prompt-matched routed trace, and the aligned-Gemma safety lane still has a role-collapsed mediator partition despite real intervention-conditioned trajectory movement.
- Critical reminder: the next repo move should change a live scientific ambiguity, not reopen a frozen one. Keep the strong Figure 8 lane frozen unless a genuinely more faithful proxy path becomes concrete, keep the safety lane honest about the current triplet-structured prompt set, and treat `resattn-b4q` as useful infrastructure rather than as the next paper-shaping result.
