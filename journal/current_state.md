# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-3f1`
- Focus: `resattn-3f1` has now validated the aligned-Gemma refusal-feature discovery workflow; the next live choice is `resattn-du2` for the Figure 8 redesign decision, with `resattn-73l` as the new safety follow-up for causal mediator checks
- Experimental status: the `96 / 128` `registry_v4` campaign on `gpt2-xl` clears the development-model oracle-loss gate strongly against uniform and the preregistered nulls; the first sequence-level pattern slice shows weak above-random but below-threshold structure; the Gemma-2 tuned-lens pilot remains KL-strong and answer-token-weaker; the locked confirm Gemma factual-recall baseline reproduces broad KL degradation under routing with `7 / 8` tuned rank-range increase and `5 / 8` tuned final-rank worsening; the dynamic counterfactual shows routed traces remain worse than the fixed `pilot_mean_alpha` control but not worse than the prompt-permuted dynamic control on the tuned KL surface; the compact-subword Figure 8 follow-ups ended in a redesign decision rather than a clean pass; and the first aligned-Gemma safety artifact now passes the workflow gate with `36 / 36` prompt-behavior checks, refusal localization at assistant-prefill layer `22`, harmfulness localization at instruction-final layer `25`, confirm pair accuracy `= 1.0` for both primary directions, and near-orthogonal refusal versus harmfulness directions (`cosine = 0.0064`)
- Critical reminder: keep the oracle-alpha framing honest, preserve the KL-primary tuned-lens metric hierarchy, treat the safety result as workflow validation rather than a mediator-conditioned routing claim, and do not treat the local AttnRes proxy artifacts as Figure 8 alignment passes until the proxy metrics themselves look credible and the proxy remains competitive with its matched baseline
