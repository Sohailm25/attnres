# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-ojq` has now landed as the grouped-view pattern-analysis robustness pass; the next overall live choice is `resattn-h1p`, while the next Figure 8-specific decision issue is `resattn-8xu`
- Experimental status: the `96 / 128` `registry_v4` campaign on `gpt2-xl` clears the development-model oracle-loss gate strongly against uniform and the preregistered nulls; the raw prereg-scale pattern slice survives grouped-view and resampling controls but remains too weak and too `k = 2`-dominated for the prereg block-structure gate; grouped-source compressions reveal stronger coarse source-type routing structure above matched random controls; the Gemma-2 tuned-lens pilot remains KL-strong and answer-token-weaker; the locked confirm Gemma factual-recall baseline reproduces broad KL degradation under routing with `7 / 8` tuned rank-range increase and `5 / 8` tuned final-rank worsening; the dynamic counterfactual shows routed traces remain worse than the fixed `pilot_mean_alpha` control but not worse than the prompt-permuted dynamic control on the tuned KL surface; the widened compact-subword `wikitext-103` Figure 8 best-checkpoint artifact softens the routed loss gap but still loses to the matched baseline and keeps the entropy ordering inverted; and the aligned-Gemma causal mediator artifact shows refusal-direction effects on the frozen confirm split with refusal-suppression preference delta `-0.1891`, harmful-context refusal-injection preference delta `+0.1741`, one greedy harmful-context flip, and flat matched harmfulness controls
- Critical reminder: keep the oracle-alpha framing honest, preserve the KL-primary tuned-lens metric hierarchy, treat the grouped-view pattern result as coarse-structure evidence rather than a block-structure pass, do not treat the local AttnRes proxy artifacts as Figure 8 alignment passes, and use `resattn-h1p` rather than another clustering cleanup as the next major lane move
