# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-18
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: there is no new broad rerun to launch right now. The main story is already in the saved Gemma oracle synthesis artifact, and the tool-breakage lane is now frozen at its truthful family-conditioned boundary.
- Experimental status: the center of gravity stays on the primary Gemma oracle lane. The narrowed `v5` factual bridge is clean for the core families, the donor audit froze the main donor-arm claim, and the old `v4` moon sidecar is now frozen as a surface-design problem rather than a clean null.
- Critical reminder: do not drift back to pooled reruns, donor-remap-by-inertia, moon redesigns on the old `v4` surface, or frozen Figure 8 rescue work. Figure 8 stays frozen.
