# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-73l`
- Focus: `resattn-73l` has now landed as the bounded aligned-Gemma causal mediator pass; the next overall live choice is `resattn-fby`, while the next safety-specific follow-up is `resattn-h1p` for mediator-conditioned routing analysis
- Experimental status: the `96 / 128` `registry_v4` campaign on `gpt2-xl` clears the development-model oracle-loss gate strongly against uniform and the preregistered nulls; the first sequence-level pattern slice shows weak above-random but below-threshold structure; the Gemma-2 tuned-lens pilot remains KL-strong and answer-token-weaker; the locked confirm Gemma factual-recall baseline reproduces broad KL degradation under routing with `7 / 8` tuned rank-range increase and `5 / 8` tuned final-rank worsening; the dynamic counterfactual shows routed traces remain worse than the fixed `pilot_mean_alpha` control but not worse than the prompt-permuted dynamic control on the tuned KL surface; the widened compact-subword `wikitext-103` Figure 8 run narrowed the baseline gap from `+0.0598` to `+0.0386` and improved deep embedding persistence from `0.1515` to `0.1615`, but it still finished behind the matched baseline and left the entropy ordering inverted; and the aligned-Gemma causal mediator artifact now shows refusal-direction effects on the frozen confirm split with refusal-suppression preference delta `-0.1891`, harmful-context refusal-injection preference delta `+0.1741`, one greedy harmful-context flip, and flat matched harmfulness controls
- Critical reminder: keep the oracle-alpha framing honest, preserve the KL-primary tuned-lens metric hierarchy, treat the new safety result as causal mediator evidence rather than a routing claim, and do not treat the local AttnRes proxy artifacts as Figure 8 alignment passes; the next honest Figure 8 question is checkpoint-level optimization dynamics, not another blind same-regime scale-up
