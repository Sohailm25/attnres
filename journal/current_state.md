# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-17
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: `resattn-js8` is now green and the next overall scientific priority is `resattn-2sb`, the first primary-model Gemma prereg-scale pattern analysis on the saved oracle artifact, with `resattn-1lk` and `resattn-mo5` still the next cross-lane strategic questions
- Experimental status: the biggest oracle-paper weakness from the last external review is now materially reduced because the primary `google/gemma-2-2b` spine not only passes exact model-backed reconstruction, a bounded `2`-prompt oracle slice, and the full saved `8`-prompt pilot stability suite, but also clears the held-out predictiveness check on the saved `96 / 128` `registry_v4` split with predicted mean improvement `+1.0428` nats, oracle mean improvement `+2.5525` nats, and `123 / 128` confirm prompts positive under the predicted alpha vectors; the remaining primary-model oracle tension is that routed-loss recovery is strong while the Euclidean alpha-shape metrics are still mixed (`R^2 = -0.0632`, mean JS `= 0.1478`), so the next honest oracle question is primary-model structure rather than more small predictor tweaks; the raw prereg-scale pattern slice on `gpt2-xl` still survives grouped-view and resampling controls but remains too weak and too `k = 2`-dominated for the prereg block-structure gate; grouped-source compressions still reveal stronger coarse source-type routing structure above matched random controls; the Gemma factual-recall tool-breakage lane remains a bounded mixed result because routed traces degrade the original-model baseline but not more than the prompt-permuted dynamic control; the widened compact-subword `wikitext-103` Figure 8 lane remains operationally real but strategically frozen for strong claims on the current tiny proxy; and the aligned-Gemma mediator-conditioned routing artifact now shows large final-layer refusal-trajectory shifts under refusal-direction interventions even though the mediator-active confirm partition still collapses exactly onto refusal prompts
- Critical reminder: do not overread the slightly negative `R^2`; the codified primary metric for predictiveness is routed-loss recovery, and that now passes on the primary model at prereg-scale prompt counts; the next right move is primary-model pattern analysis on the saved artifact, not reopening frozen mixed lanes or running more tiny feature sweeps; preserve the KL-primary tuned-lens metric hierarchy, treat the grouped-view pattern result as coarse-structure evidence rather than a block-structure pass, keep the Figure 8 lane in freeze-or-escalate territory rather than iterative rescue, and do not overstate the safety lane while the current mediator partition remains role-collapsed
