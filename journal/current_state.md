# ABOUTME: Tracks the repo state relative to the research plan for quick session recovery.
# ABOUTME: Overwritten as the experiment state changes so future sessions can restart without rereading the full repo.

# Journal Current State

- Date: 2026-03-18
- Repo: standalone and initialized
- Branch: `wip/resattn-scaffold`
- Focus: there is no new broad rerun to launch right now. The main story is already in the saved Gemma oracle synthesis artifact, and the next core lane is Phase 6 data readiness rather than another extension rerun.
- Experimental status: the center of gravity stays on the primary Gemma oracle lane. Tool-breakage is frozen at a truthful boundary, and the next honest core move is pilot-only per-token export because the current campaign still saves only sequence-level supervision.
- Critical reminder: do not drift back to pooled reruns, donor-remap-by-inertia, moon redesigns on the old `v4` surface, or frozen Figure 8 rescue work. The current blocker is missing per-token export, not missing oracle signal.
