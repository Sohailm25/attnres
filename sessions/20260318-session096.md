ABOUTME: Session log for the Phase 6 per-token export implementation slice.
ABOUTME: Tracks the TDD implementation and real Gemma pilot export for router-distillation readiness.

# Session Log: 20260318-session096

**Date:** 2026-03-18
**Agent:** codex-gpt5
**Phase:** Phase 6 readiness implementation
**Duration:** completed

## Objective

Land `resattn-1ot` by adding the smallest honest pilot-only per-token export
path for Gemma router distillation.

## Thesis Lanes Touched

- [x] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [ ] tool-breakage
- [x] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Created `wip/resattn-1ot`
- [x] Re-read the campaign checkpoint code, runner code, and readiness audit
- [x] Added the initial failing test surface for the export path
- [x] Implemented the dedicated export module and CLI entry point
- [x] Verified the focused unit tests after the final schema patch
- [x] Ran the real Gemma calibration export and resume check
- [x] Ran the full `256`-prompt pilot export and refreshed it after adding metadata fields
- [x] Verified exact-command resume reuse on the completed full output

## Key Findings

- The export should be a dedicated pilot-only path, not a mutation of the
  large sequence-level summary artifacts.
- The right artifact split is:
  - ignored per-prompt `.pt` checkpoints
  - committed manifest and summary JSON/MD
- The final saved dataset surface is large enough for the first honest Phase 6
  pilot:
  - `256` prompts
  - `4013` total tokens
  - balanced `64 x 4` stratum coverage
- Schema invalidation mattered. Without it, the repo could have ended up with a
  committed manifest that described fields not actually present in older saved
  prompt checkpoints.

## Issues Encountered

- The first real calibration launch failed with `ModuleNotFoundError` because
  the top-level script needed the repo root on `sys.path`.
- After adding `tags` and `perturbations`, the original completed export had to
  be refreshed in place so the prompt checkpoints matched the committed schema.

## Next Steps

- Close `resattn-1ot`.
- Start `resattn-m6r`: pilot router distillation on the exported dataset with
  the prereg `h_1[t]` versus `h_4[t]` comparison.
