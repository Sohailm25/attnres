# Session Log: 20260317-session025

**Date:** 2026-03-17
**Agent:** codex-gpt5
**Phase:** Phase 1 - Oracle-alpha infrastructure, reconstruction sanity checks, and stability gates
**Duration:** ~40 minutes

## Objective

Launch the first prereg-scale oracle-alpha predictiveness campaign on `registry_v4` after the checkpointed campaign build-out and prompt-surface freeze landed.

## Thesis Lanes Touched

- [x] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [ ] tool-breakage
- [ ] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Create `wip/resattn-9jq-launch`.
- [x] Log a calibration run and the full tmux launch plan in `SCRATCHPAD.md`.
- [x] Verify calibration checkpoint materialization on the final output directory.
- [x] Verify the exact calibration resume command reuses cached checkpoint files without rewriting them.
- [x] Launch the full `96 / 128` `gpt2-xl` campaign in tmux session `oa-v4-prereg-scale`.
- [x] Wait for the full prereg-scale campaign to finish and inspect the final summary outputs.
- [x] File the summary-stage observability follow-up as `resattn-9co`.

## Key Findings

- The prompt-level oracle checkpointing and feature-cache path is real: calibration wrote the expected files, and the resume command reused them.
- The prereg-scale development-model oracle stage cleared the confirm-split gate strongly: `128` confirm prompts, mean oracle improvement over uniform `= +1.2993` nats, `128 / 128` prompts positive, and all preregistered null baselines worse than oracle alpha on every confirm prompt.
- Held-out predictiveness stayed positive at prereg scale, but the selector moved back to the raw-simplex target with heavy shrinkage: predicted mean improvement over uniform `= +0.1162` nats, `95 / 128` prompts positive, confirm `R^2 = -0.0884`, mean JS `= 0.2427`, selected target `oracle_alpha_vector`, `lambda = 100.0`.
- The summary-stage sweep is materially slower than the oracle checkpointing stage and leaves stale top-level summary files until it finishes, which is now logged as `resattn-9co`.

## Artifacts Produced

- `results/oracle_alpha/20260317-gpt2xl-prereg-scale-campaign-v4/`
- `results/oracle_alpha/20260317-gpt2xl-prereg-scale-campaign-v4.md`
- `sessions/20260317-session025.md`

## Issues Encountered

- The first tmux launch completed the oracle checkpoints and cached features well before the final summary files updated, which made the live output look partially stale until the summary sweep finished.
- A duplicate foreground rerun was started briefly to test whether the stale-summary state was a hard failure; it was then killed once it became clear that the summary stage itself is just expensive at this prompt scale.

## Next Steps

- Register the prereg-scale oracle artifact in the results index and control docs.
- Close `resattn-9jq` and move the next oracle task to prereg-scale pattern analysis on the saved `registry_v4` artifact.
- Keep strong interpretation blocked until later work either improves alpha-shape recovery or clarifies that routed-loss-positive predictiveness is the right confirmatory object.

## Files Modified

- `SCRATCHPAD.md` - calibration and full-launch entries for the prereg-scale campaign
- `sessions/20260317-session025.md` - session log for the prereg-scale campaign launch and result
