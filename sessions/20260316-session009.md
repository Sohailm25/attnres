# Session Log: 20260316-session009

**Date:** 2026-03-16
**Agent:** codex-gpt5
**Phase:** Phase 1 - Pilot/confirmatory split definition and enforcement
**Duration:** complete

## Objective

Take `resattn-gke` and add the smallest durable pilot/confirmatory split system that saves a versioned prompt registry and enforces confirm-only access for claim-bearing runs.

## Thesis Lanes Touched

- [x] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [ ] tool-breakage
- [ ] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Re-read the control docs and split-related guidance after the Phase 1 reconstruction slice
- [x] Add failing tests for the versioned registry and confirm-access guard
- [x] Save the initial pilot/confirm prompt registry artifact
- [x] Update state docs and merge the task branch back into `wip/resattn-scaffold`
- [x] Close `resattn-gke`

## Key Findings

- No saved prompt split or registry implementation exists yet.
- The current repo already commits to a saved split plus code-level confirm-only enforcement, so the task is now implementation rather than policy discussion.
- The first script-level guard check failed because direct execution did not add the repo root to `sys.path`; a subprocess-backed test now covers that failure mode.

## Artifacts Produced

- `sessions/20260316-session009.md`
- `prompts/registry_v1.yaml`
- `prompts/registry.py`
- `scripts/export_prompt_split.py`
- `tests/test_prompt_registry.py`
- `results/infrastructure/20260316-pilot-confirm-registry.md`

## Issues Encountered

- The first direct script execution failed with `ModuleNotFoundError: No module named 'prompts'` because Python treated `scripts/` rather than the repo root as the import base.

## Next Steps

- Add the identifiability, MIB, and out-of-sample predictiveness controls in the next Phase 1 slice.
- Keep the prompt registry expandable, but avoid reopening the confirm-access rule.

## Files Modified

- `sessions/20260316-session009.md` - started the session log for `resattn-gke`
- `prompts/__init__.py` - exported the prompt registry helpers
- `prompts/registry.py` - implemented the versioned registry loader and confirm-only access rule
- `prompts/registry_v1.yaml` - saved the initial oracle-alpha and tool-breakage pilot/confirm prompt collections
- `scripts/export_prompt_split.py` - added a scriptable access path that enforces the confirm lock
- `tests/test_prompt_registry.py` - added unit and subprocess tests for the registry and export script
- `tests/test_scaffold.py` - extended the scaffold invariants to cover the prompt registry integration
- `configs/experiment.yaml` - pointed the main config at the saved prompt registry
- `CURRENT_STATE.md` - recorded that the split blocker is cleared and the next blocker is the identifiability suite
- `journal/current_state.md` - refreshed the running branch and focus note after landing `resattn-gke`
- `DECISIONS.md` - logged the registry-format and confirm-lock decision
- `THOUGHT_LOG.md` - captured the qualitative lesson from turning the split into an enforced artifact
- `results/RESULTS_INDEX.md` - registered the new infrastructure artifact
- `results/infrastructure/20260316-pilot-confirm-registry.md` - summarized the saved split and enforcement result
