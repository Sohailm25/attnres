ABOUTME: Session log for the Phase 6 pilot router-distillation comparison.
ABOUTME: Tracks the smallest honest h_1[t] versus h_4[t] router pilot on the saved Gemma export.

# Session Log: 20260318-session097

**Date:** 2026-03-18
**Agent:** codex-gpt5
**Phase:** Phase 6 pilot router distillation
**Duration:** completed

## Objective

Land `resattn-m6r` by fitting the first pilot-only Gemma router distillation
comparison on the saved per-token export and choosing between `h_1[t]` and
`h_4[t]` honestly before any confirmatory router training.

## Thesis Lanes Touched

- [x] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [ ] tool-breakage
- [x] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Created `wip/resattn-m6r`
- [x] Re-read the Phase 6 prereg, current-state, and router-export docs
- [x] Added the initial failing tests for the router pilot
- [x] Implemented the minimal pilot router-distillation module and CLI
- [x] Ran the real Gemma pilot comparison on the full saved export

## Key Findings

- The data blocker is gone. The saved Gemma pilot export is the Phase 6
  dataset of record.
- The first real modeling fork is sequence aggregation from per-token router
  outputs back to the saved sequence-level `final_alpha` target.
- The first router pilot does not support an input lock:
  - `h_4[t]` only narrowly beats `h_1[t]`
  - both inputs fail the held-out pilot gate with negative `R^2`
  - both inputs collapse toward a near-uniform predicted mixture
  - rerunning after compacting the saved summary artifact moved the exact numbers slightly on MPS without changing the qualitative result

## Issues Encountered

- The first pilot router fit was not blocked by runtime or data plumbing. The
  actual blocker is modeling collapse under the current raw-alpha MSE setup.

## Next Steps

- Close `resattn-m6r`.
- Start `resattn-4hj`: compare target parameterizations on the same saved pilot
  export before revisiting either the input lock or the geometry lane.
