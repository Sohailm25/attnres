ABOUTME: Session log for the route-mode coverage audit on the one-token factual tool-breakage surface.
ABOUTME: Narrows the next extension step from family labels to explicit missing route modes.

# Session Log: 20260318-session086

**Date:** 2026-03-18
**Agent:** codex-gpt5
**Phase:** Primary-model oracle bridge analysis
**Duration:** completed

## Objective

Run `resattn-unp`: audit how the one-token `tool_breakage_factual_recall_v4`
surface covers the saved factual route modes from the Gemma `registry_v5`
oracle artifact, then decide whether the next extension should stay on the
current surface or move to a route-mode-aware redesign.

## Thesis Lanes Touched

- [x] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [x] tool-breakage
- [ ] `w_l` analog geometry
- [x] block structure
- [ ] safety alignment

## Work Completed

- [x] Added reusable route-mode coverage summaries on top of the existing bridge helper.
- [x] Added a saved-artifact runner for route-mode coverage against the `v4` tool-breakage surface.
- [x] Ran the real `v4` coverage audit on the saved artifacts.
- [x] Wrote the artifact memo and set the next bridge issue.

## Key Findings

- The current one-token `v4` surface undercovers the saved factual route-mode map:
  - family-conditioned factual modes `= 13`
  - modes with any `v4` assignment `= 8 / 13`
  - modes with matching-family `v4` assignment `= 5 / 13`
- Capitals and elements are both collapsed to one covered mode each:
  - all `12` capital prompts nearest-match capital mode `cluster 3`
  - all `12` element prompts nearest-match element mode `cluster 1`
- Authors cover only `2 / 5` author modes, and `7` author prompts nearest-match
  the dominant capital mode instead.
- The next extension should stay on capitals, elements, and authors, but it
  should not stay on the current `v4` surface unchanged.

## Artifacts Produced

- `results/tool_breakage/20260318-gemma2-tool-breakage-route-mode-coverage-v4.json`
- `results/tool_breakage/20260318-gemma2-tool-breakage-route-mode-coverage-v4.md`
- `sessions/20260318-session086.md`

## Issues Encountered

- The first real output showed that one author outlier sits inside a
  moon-dominant raw cluster, so `cluster_label` alone was not a safe unique mode
  identifier for this bridge. The coverage summary was corrected to key modes by
  `(family, cluster)` rather than pretending one cluster label implies one
  family mode.

## Next Steps

- Close `resattn-unp`.
- Take `resattn-q38` next to redesign the one-token factual surface around the
  missing capital, element, and author modes.
- Keep `resattn-a1w` as the moon-specific sidecar only.

## Files Modified

- `validation/tool_breakage_bridge.py` - route-mode coverage summary
- `scripts/run_tool_breakage_route_mode_bridge.py` - saved-artifact runner
- `tests/test_tool_breakage_bridge.py` - TDD coverage for route-mode coverage
- `results/tool_breakage/20260318-gemma2-tool-breakage-route-mode-coverage-v4.json` - saved summary
- `results/tool_breakage/20260318-gemma2-tool-breakage-route-mode-coverage-v4.md` - artifact write-up
- `CURRENT_STATE.md` - next-step alignment
- `DECISIONS.md` - `unp` closeout decision
- `THOUGHT_LOG.md` - route-mode coverage reflection
- `journal/current_state.md` - new main bridge focus
- `results/RESULTS_INDEX.md` - artifact registration
