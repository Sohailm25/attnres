# Session Log: 20260318-session110

**Date:** 2026-03-18
**Agent:** codex-gpt5
**Phase:** Phase 4 router training
**Duration:** in progress

## Objective

Land `resattn-7xo` by comparing the retained all-token Gemma router baseline
against a small exact tokenwise-oracle teacher slice on a deterministic bounded
subset of the frozen `192 / 64` pilot split.

## Thesis Lanes Touched

- [ ] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [ ] tool-breakage
- [x] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Read the issue and re-check the current Phase 6 state after `resattn-afu`
- [x] Add failing tests for exact tokenwise oracle teachers on a bounded subset
- [x] Implement the smallest exact-teacher path and run the subset comparison
- [x] Calibrate the exact-teacher runtime before the real bounded subset launch

## Key Findings

- The bounded shared-final-norm contribution teacher from `resattn-afu` failed
  badly, so the next honest question is exact tokenwise oracle fidelity on a
  smaller subset, not another approximation and not more architecture churn.
- The clean bounded design is:
  - retain the saved all-token MLP baseline
  - keep the repeated sequence-target controls
  - regenerate exact per-token oracle alpha-logit teachers on a deterministic
    stratum-balanced subset only
- The exact teacher is a real negative result on the bounded `32 / 16` subset:
  - `all_tokens_target_mse`: `R^2 = 0.1567`, mean JS `= 0.1014`
  - `next_token_positions_sequence_target_mse`: `R^2 = 0.1803`, mean JS `= 0.0998`
  - `next_token_positions_exact_oracle_alpha_logit_mse`: `R^2 = -0.1126`, mean JS `= 0.1486`
- The only positive movement on this bounded slice is the weaker support-only
  change from supervising only next-token-valid positions.
- The larger full-split result from `resattn-afu` still matters more than this
  subset reversal for choosing the saved default baseline, so the repo should
  not redesign the export around tokenwise teachers from this result.
- The exact-command rerun preserved the `summary.json` hash on MPS:
  - `ff6c608adfbb05dcf5895a11ce8740553304f9b2`

## Artifacts Produced

- `sessions/20260318-session110.md`
- `results/router_training/20260319-gemma2-router-distillation-exact-teacher-subset-v1/summary.json`
- `results/router_training/20260319-gemma2-router-distillation-exact-teacher-subset-v1.md`

## Issues Encountered

- The exact-teacher path is meaningfully slower than the earlier approximate
  path, so a tiny calibration slice was necessary before the real `32 / 16`
  subset run.

## Next Steps

- Close `resattn-7xo` as a stable negative result for exact tokenwise teacher
  rescue on the bounded Gemma subset.
- Keep the retained broader Phase 6 baseline on the larger saved split.
- Take `resattn-b4h` next to diagnose whether exact-tokenwise failure is driven
  by within-prompt teacher variance or by sequence-aggregation mismatch.

## Files Modified

- `sessions/20260318-session110.md` - session log for `resattn-7xo`
- `tests/test_router_distillation.py` - added exact-teacher regression tests and exact-objective coverage
- `validation/router_distillation.py` - added exact tokenwise teacher generation and objective support
- `scripts/run_router_distillation_exact_teacher_subset_comparison.py` - bounded exact-teacher subset runner
- `SCRATCHPAD.md` - calibration and real subset run records
