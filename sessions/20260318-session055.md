ABOUTME: Session log for the primary-model Gemma routing-regime comparison implementation.
ABOUTME: Tracks the runner extension and first primary-model softmax versus unconstrained versus top-k artifact.

# Session Log: 20260318-session055

**Date:** 2026-03-18
**Agent:** codex-gpt5
**Phase:** Primary-model comparison regimes
**Duration:** completed

## Objective

Advance `resattn-5eo` by extending the oracle runner to support the prereg
softmax-constrained, unconstrained, and top-k routing regimes, then run the
first primary-model Gemma comparison on the saved `registry_v4` surface.

## Thesis Lanes Touched

- [x] oracle-alpha
- [ ] pattern analysis
- [ ] Figure 8 validation
- [x] comparison regimes
- [ ] tool-breakage
- [ ] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Read the prereg and decision-matrix definitions for the three routing
      regimes.
- [x] Added failing runner tests for regime transforms and matched-zero
      initialization behavior.
- [x] Implemented the minimal regime-aware oracle optimization path.
- [x] Ran and registered the first primary-model Gemma regime comparison.

## Key Findings

- The first hard-top-k pass was optimization-invalid under exact zero
  initialization because the arbitrary initial support got all the gradient.
- After repairing support search with an exact-forward straight-through top-k
  path, the full Gemma regime comparison came back decisive:
  - softmax-constrained `= +2.5525` nats over uniform
  - unconstrained `= +2.0637`
  - top-k rose monotonically from `+0.0115` at `k = 2` to `+1.4664` at
    `k = 26`
  - softmax beat every alternative on all `128` confirm prompts

## Artifacts Produced

- `sessions/20260318-session055.md`
- `results/comparison_regimes/20260318-gemma2-regime-comparison-v1/summary.json`
- `results/comparison_regimes/20260318-gemma2-regime-comparison-v1.md`

## Issues Encountered

- The current oracle runner only implements the softmax-constrained path, so
  this issue starts with a real code-path gap rather than a pure analysis run.
- The first top-k implementation was not scientifically valid under the matched
  `z = 0` start and had to be repaired before the comparison could be trusted.

## Next Steps

- Close `resattn-5eo` and update the core state docs with the primary-model
  regime-separation result.
- Move to `resattn-1lk`, the conservative Figure 8 freeze-or-escalate decision.

## Files Modified

- `sessions/20260318-session055.md` - session log for `resattn-5eo`
- `validation/oracle_alpha_runner.py` - regime-aware alpha transforms and the
  repaired top-k support-search path
- `validation/comparison_regimes.py` - checkpointed regime scheduling and compact
  summary writing
- `tests/test_oracle_alpha_runner.py` - matched-zero regime coverage and the
  top-k gradient regression test
- `tests/test_comparison_regimes.py` - regime summary coverage
- `tests/test_scaffold.py` - runner registration
- `scripts/run_oracle_alpha_regime_comparison.py` - Gemma regime launch script
