# Session Log: 20260317-session030

**Date:** 2026-03-17
**Agent:** codex-gpt5
**Phase:** Phase 3 - Tool-breakage and safety routing analysis
**Duration:** ongoing

## Objective

Advance `resattn-28b` by implementing the first same-model Gemma-2 routed-versus-original factual-recall tool-breakage baseline, using the custom tuned lens from `resattn-5k9` and the KL-primary metric hierarchy from `resattn-ehz`.

## Thesis Lanes Touched

- [ ] oracle-alpha
- [ ] Figure 8 validation
- [ ] comparison regimes
- [x] tool-breakage
- [ ] `w_l` analog geometry
- [ ] block structure
- [ ] safety alignment

## Work Completed

- [x] Re-read the current tool-breakage state, prereg wording, and tuned-lens viability artifact.
- [x] Added `target_text` metadata to the saved factual-recall prompt registry and enforced it in the prompt loader for the tool-breakage lane.
- [x] Added TDD coverage for prompt metadata, routed residual traces, correct-token tokenization, and prompt/run summarization.
- [x] Implemented `validation/tool_breakage.py` plus `scripts/run_tool_breakage_factual_recall_baseline.py`.
- [x] Ran a one-prompt calibration to verify prompt checkpoints, routed-versus-original KL separation, and resume behavior.
- [x] Launched the full `8`-prompt pilot in `tmux`, verified prompt-level checkpoints, and analyzed the resulting artifact.
- [x] Filed `resattn-ypj` to harden the post-pilot metric surface before any confirm run.

## Key Findings

- The first same-model Gemma factual-recall baseline runner now exists and is checkpointed at the prompt level.
- The `8`-prompt pilot shows broad KL-primary degradation under routing:
  - tuned mean KL worsened on `8 / 8` prompts
  - tuned final-position KL worsened on `8 / 8` prompts
  - raw mean KL worsened on `7 / 8` prompts
  - raw and tuned mean top-1 both worsened on `8 / 8` prompts
- The legacy non-monotonicity boolean is not useful on this pilot because the original-model baseline is already saturated:
  - raw original non-monotonic on `8 / 8`
  - tuned original non-monotonic on `8 / 8`
  - raw routed non-monotonic on `8 / 8`
  - tuned routed non-monotonic on `8 / 8`
- Relative target-rank degradation is mixed rather than saturated:
  - final-layer tuned target rank worsened on `4 / 8` prompts
  - best-layer tuned target rank also worsened on `4 / 8` prompts
- The correct interpretation is that `resattn-28b` is a baseline implementation pass, not a strong-claim pass.

## Artifacts Produced

- `sessions/20260317-session030.md`
- `results/tool_breakage/20260317-gemma2-tool-breakage-baseline-pilot-v1.md`
- `results/tool_breakage/20260317-gemma2-tool-breakage-baseline-pilot-v1/summary.json`

## Issues Encountered

- The first runner version incorrectly tried to route through the oracle-alpha control registry for the factual-recall prompt collection. That failed because the collection is not part of the oracle-alpha control plan.
- I fixed that by moving the small per-prompt oracle-alpha optimization needed for the baseline directly into `validation/tool_breakage.py`, so the tool-breakage runner no longer depends on the oracle-alpha collection registry.
- The pilot also exposed a scientific issue rather than a code bug: the prereg boolean non-monotonicity threshold saturates on the original-model baseline for this Gemma factual-recall surface.

## Next Steps

- Close `resattn-28b` once the state docs, results index, and memo are landed.
- Use `resattn-ypj` to define stronger routed-versus-original success metrics from the saved pilot traces before any confirmatory Gemma factual-recall run.
- Keep the eventual strong tool-breakage claim blocked on both the metric hardening step and the later controlled dynamic-routing counterfactual.

## Files Modified

- `prompts/registry.py` - prompt metadata schema and tool-breakage validation
- `prompts/registry_v4.yaml` - saved `target_text` values for the factual-recall pilot and confirm prompts
- `tests/test_prompt_registry.py` - prompt metadata coverage
- `tests/test_scaffold.py` - new baseline runner script expectation
- `tests/test_tool_breakage.py` - tool-breakage TDD coverage
- `validation/tool_breakage.py` - checkpointed routed-versus-original baseline logic
- `validation/__init__.py` - tool-breakage exports
- `scripts/run_tool_breakage_factual_recall_baseline.py` - CLI launch path
- `results/tool_breakage/20260317-gemma2-tool-breakage-baseline-pilot-v1.md` - pilot memo
- `SCRATCHPAD.md` - calibration and full-pilot run logs
